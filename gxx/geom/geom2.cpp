#include <gxx/geom/geom2.h>
#include <gxx/x11wrap/shower2d.h>

namespace gxx {
	namespace geom2 {
		/*void line::drawTo(drawer2d& cntxt) const {
			if (!trimmed) {
				cntxt.add_line(d0(-1000), d0(1000));
			} else {
				cntxt.add_line(d0(bmin), d0(bmax));	
			}
		}

		void circle::drawTo(drawer2d& cntxt) const {
			cntxt.add_arc(l.x - r, l.y + r, 2*r, 2*r, M_PI * 0, M_PI * 2 * 64);
		}*/
	}
}