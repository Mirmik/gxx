local files = find.findInTree(script.currentDir .. "gxx", ".*.gll$", ".*HIDE.*")
script:evalFile(files, _ENV)

Module("gxx.sources", {
	sources = {
		directory = "gxx", 
		cxx = "buffer.cpp format.cpp arglist.cpp"
	}	
})

Module("gxx.include", {
	depends = "gxx.sources",
	includePaths = "."
}) 