local files = find.findInTree(script.currentDir .. "/gxx", ".*.gll$", ".*HIDE.*")
script:evalFile(files, _ENV)

Module("gxx.sources", {
	flags = {
		cxx = "-Wno-unused-local-typedefs"
	},
	sources = {
		directory = "gxx", 
		cxx = "format.cpp arglist.cpp logger/stdout.cpp print.cpp " ..
		"serialize/json.cpp serialize/datatree.cpp",
--		cc = "gmsgpack/gmsgpack.c"
	}	
})

Module("gxx.include", {
	--depends = "gxx.sources",
	includePaths = "."
}) 

Module("gxx.log_manager", {
	sources = {
		directory = "gxx/logger", 
		cxx = "manager.cpp"
	}
})

Module("gxx.placed_new", {
	sources = {
		directory = "gxx/util", 
		cxx = "placed_new.cpp"
	}
})

Module("gxx.namespace_std", {
	includePaths = "gxx/std/"
})